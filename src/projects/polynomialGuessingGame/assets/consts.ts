export const maxCoeff = 5;
export const minCoeff = -5;
export const minX = -4;
export const maxX = 4;

export type Polynomial = {
  a: number;
  b: number;
  c: number;
  d: number;
};

export type Question = {
  x: number;
  y: number;
};
